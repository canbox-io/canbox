# launcher

## 概述

在 Canbox 中新增启动器（Launcher）功能，类似 uTools 的快速启动器。用户按全局快捷键弹出搜索框，快速搜索并启动应用（Canbox 已安装应用 + Linux 系统应用）。

## 核心设计

### 功能范围

- **设置页面**：在 `Settings.vue` 添加"启动器"配置分组
- **全局快捷键**：用户可自定义，默认 `Alt+Space`
- **启动器窗口**：无边框、始终置顶、不在任务栏显示
- **应用搜索**：支持中文应用名的拼音匹配（全拼 + 首字母）
- **系统应用读取**：解析 Linux 的 `.desktop` 文件（Phase 1 必须包含）

### 窗口行为

| 行为 | 触发条件 |
|------|----------|
| 显示窗口 | 按全局快捷键 |
| 隐藏窗口 | 失去焦点 / 按 `Esc` / 启动应用后 |
| 启动应用 | 按 `Enter` / 点击应用图标 |

### 窗口样式

- **位置**：屏幕横向居中，纵向距顶三分之一
- **大小**：默认宽度 600px，高度 400px
- **字体**：默认 16px
- **圆角**：默认 12px
- **动画**：无，直接显示/隐藏

### 搜索逻辑

- **无输入时**：显示系统应用列表的前 5 个（按 `.desktop` 文件读取顺序）
- **有输入时**：模糊匹配（中文名、拼音全拼、拼音首字母），显示最匹配的 5 个结果

### 系统应用读取范围（Linux）

| 路径 | 说明 |
|------|------|
| `~/.local/share/applications/` | 用户级应用 |
| `/usr/share/applications/` | 系统级应用 |
| `/var/lib/flatpak/exports/share/applications/` | Flatpak 系统级 |
| `~/.local/share/flatpak/exports/share/applications/` | Flatpak 用户级 |

### 配置文件存储

使用 `canbox.json` 存储启动器配置：

```json
{
  "launcher": {
    "enabled": true,
    "shortcut": "Alt+Space",
    "position": "top-third",
    "width": 600,
    "fontSize": 16,
    "borderRadius": 12,
    "extraShortcutDirs": []
  }
}
```

## 验收标准

- [x] 按全局快捷键能弹出启动器窗口
- [x] 窗口位置正确：横向居中，纵向距顶三分之一
- [x] 搜索应用能正确显示结果（包括中文应用名的拼音匹配）
- [x] 失去焦点 / 按 `Esc` / 启动应用后窗口自动隐藏
- [x] 设置页面能配置启动器（启用/禁用、快捷键、样式）
- [x] 快捷键冲突时能提示用户
- [x] 系统应用能正确读取（包括 Flatpak 应用）
- [x] 与现有功能无冲突（特别是开机自启、托盘功能）

## 实施计划

- [x] 创建 `modules/launcher/` 目录
- [x] 实现 `launcherManager.js`（窗口管理、快捷键注册）
- [x] 实现 `systemAppReader.js`（解析 `.desktop` 文件）
- [x] 实现 `appSearchEngine.js`（模糊搜索 + 拼音匹配）
- [x] 创建 `src/components/Launcher.vue`（搜索界面）
- [x] 在 `Settings.vue` 添加"启动器"配置分组
- [x] 在 `preload.js` 添加 `window.api.launcher` API
- [x] 在 `ipcHandlers.js` 添加启动器相关 IPC handlers
- [x] 在 `main.js` 中初始化 `LauncherManager`
- [x] 确保应用安装后自动创建快捷方式（实际修改 `modules/main/appManager.js` 而非 `appManagerIpcHandler.js`）

## 实际变更文件

### 新增
| 文件 | 说明 |
|------|------|
| `modules/launcher/launcherManager.js` | 启动器窗口管理、全局快捷键注册/注销 |
| `modules/launcher/systemAppReader.js` | Linux `.desktop` 文件解析，含 Flatpak 支持 |
| `modules/launcher/appSearchEngine.js` | 模糊搜索、中文拼音匹配（全拼+首字母）、子序列匹配 |
| `src/components/Launcher.vue` | 启动器搜索 UI（无边框、置顶、按 Esc/失焦隐藏） |

### 修改
| 文件 | 变更说明 |
|------|---------|
| `main.js` | 引入 LauncherManager，init()/destroy() 生命周期 |
| `ipcHandlers.js` | 新增 10 个启动器 IPC handler（getConfig/setConfig/checkShortcut/toggle/hide/getAllApps/searchApps/launchApp/getAppIcon）+ 全局 zoom 限制广播 |
| `preload.js` | 新增 `window.api.launcher` 命名空间（9 个 API + launcher:shown 事件） |
| `src/components/Settings.vue` | 新增"🚀 启动器"配置分组（启用开关、快捷键选择/自定义、窗口宽度/字号/圆角 slider） |
| `locales/zh-CN.json` | 新增 `launcher.*` 中文语言 key |
| `locales/en-US.json` | 新增 `launcher.*` 英文语言 key |
| `modules/main/appManager.js` | `importAppFromZip` 中自动调用 `generateShortcuts()` |
| `modules/web-app/web-app-creator.js` | WebApp 创建后自动生成快捷方式 |
| `src/main.js` | Launcher 窗口 zoom 锁定（`setVisualZoomLevelLimits(1,1)`） |

## 关键实现细节

1. **搜索引擎移至主进程**：原方案在 renderer 侧做搜索匹配，后改为主进程 `appSearchEngine.js`，通过 `launcher:searchApps` IPC 调用，避免大数据传输
2. **子序列模糊匹配**：`searchScore` 算法支持非连续字符匹配，对中文拼音场景更友好（如 "wxyy" 可匹配 "文心一言"）
3. **Flatpak 图标路径**：`resolveIconPath` 支持多级目录递归搜索（普通应用深度 5，Flatpak 深度 10）
4. **窗口独立 partition**：Launcher 设置独立 session partition，防止主窗口 Ctrl+滚轮 zoom 传染
5. **设置页防抖**：`launcherInitComplete` 标记防止 slider `@change` 在初始化时触发保存

## 已知问题

- 首次打开 Launcher 时，`systemAppReader` 同步 I/O 解析 `.desktop` 文件可能耗时数秒，导致默认应用列表延迟显示（另案优化）

---

*创建时间: 2026-06-06*
*最后更新: 2026-06-07*
*版本: 1.1*
